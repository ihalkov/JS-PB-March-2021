// let number = 101;
// if (number >= 1)
//  console.log("Larger than 1");
// if (number <= 101)
//  console.log("Less than 101");
//  console.log("Equal to 101");

 let role = "Administrator";
let password = "SoftUni";
if (role == "SoftUni") {
  if (password == "SoftUni") {
    console.log("Welcome!");
  }
}

// pre incrementation
// ++a => raise the value and return 'a'

// Post incrementation
// a++ => return 'a' and then raise the value

// Можем да вземем  символ от текст по индекс, като използваме метода charAt(index) – където index е число (индекса), от който ще вземем съответната буквата

